{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "cmake --build .",
            "type": "shell",
            "args": [],
            "problemMatcher": [
                "$msCompile"
            ],
            "options": {
                "cwd": "${workspaceRoot}\\build"
            },
            "group": "build"
        }
    ]
}