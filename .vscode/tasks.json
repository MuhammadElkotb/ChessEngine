{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "cmake --build . --config Debug -- /p:Platform=x86",
            "type": "shell",
            "args": [],
            "problemMatcher": [
                "$msCompile"
            ],
            "options": {
                "cwd": "${workspaceRoot}\\build"
            },
            "presentation": {
                "clear": true
            },
            "group": "build"
        }
    ]
}